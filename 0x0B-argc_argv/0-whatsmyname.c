#include <stdio.h>
/**
 * Main - Print name of program.
 * @argc: number of string.
 * @argv: pointer.
 * Return: never.
 */
int main(int argc, char *char *argv[])
{
	if (argc >= 0)
		printf("%s\n", argv);
	return (0);
}
