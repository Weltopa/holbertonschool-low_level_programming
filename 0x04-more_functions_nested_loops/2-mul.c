#include "holberton.h"

/**
 * mul - Multiply two integers.
 * @result - answers multiplication variable.
 * @a: multiplied variable.
 * @b: second variable.
 * Return: Never
 */
int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
